# Домашнє завдання 2. Множини: операції та спрощення виразів

**Тема:** Теорія множин

## Завдання 

### 1. Спростити вирази

1. `A \ (B ∪ C) ∪ (A \ B) ∩ (A \ C)`

 A \ (B ∪ C) ∪ (A \ B) ∩ (A \ C)
= (A ∩ B̄ ∩ C̄) ∪ (A ∩ B̄) ∩ (A ∩ C̄)
(A \ B = A ∩ B̄, теорема де Моргана)
= (A ∩ B̄ ∩ C̄) ∪ (A ∩ B̄ ∩ C̄)
= A ∩ B̄ ∩ C̄
(ідемпотентність)
= A \ (B ∪ C)

Відповідь: A \ (B ∪ C)

2. `((A \ C) \ (B \ C)) ∩ ((A \ B) \ C) ∪ A ∩ (B ∪ A)`

 ((A \ C) \ (B \ C)) ∩ ((A \ B) \ C) ∪ A ∩ (B ∪ A)
= ((A ∩ C̄) ∩ (B ∩ C̄)̄) ∩ (A ∩ B̄ ∩ C̄) ∪ A ∩ (B ∪ A)
(A \ B = A ∩ B̄)
= ((A ∩ C̄) ∩ (B̄ ∪ C)) ∩ (A ∩ B̄ ∩ C̄) ∪ A ∩ (B ∪ A)
(теорема де Моргана)
= (A ∩ B̄ ∩ C̄) ∩ (A ∩ B̄ ∩ C̄) ∪ A ∩ (B ∪ A)
= (A ∩ B̄ ∩ C̄) ∪ A ∩ (B ∪ A)
(ідемпотентність)
= (A ∩ B̄ ∩ C̄) ∪ A
= A
(закон поглинання)

Відповідь: A

### 2. Що можна сказати про множини A та B, якщо виконується задана рівність?

Для кожного пункту опишіть словами (і, за можливості, доведіть), яке
співвідношення між `A` і `B` з цього випливає.

1. `A ∸ B = B`
   
  За означенням симетричної різниці:
(A \ B) ∪ (B \ A) = B

Якщо в A існує елемент, якого немає в B, то він належить A \ B і, відповідно, лівій частині рівності. Але в правій частині B такого елемента немає.

Тому:
A \ B = ∅

Це означає, що кожен елемент A належить також B:
A ⊆ B

Відповідь: A є підмножиною B

2. `A ∸ B = ∅`
   
 За означенням симетричної різниці:
(A \ B) ∪ (B \ A) = ∅

Щоб об'єднання було порожнім, обидві множини мають бути порожніми:
A \ B = ∅
B \ A = ∅

Отже:
A ⊆ B
і
B ⊆ A

Якщо кожна множина є підмножиною іншої, то вони рівні:
A = B

Відповідь: множини A і B рівні

3. `A ∸ B = C` (для деякої фіксованої множини `C`; опишіть, що можна сказати про зв'язок `A`, `B` і `C`)
   
 За означенням симетричної різниці:
(A \ B) ∪ (B \ A) = C

Тобто до C входять ті елементи, які належать одній із множин A або B, але не належать одночасно обом

Елементи перетину A ∩ B до C не входять

Отже, C показує, які елементи відрізняють множину A від множини B

Також можна записати:

A ∸ C = B
B ∸ C = A

Відповідь: C є симетричною різницею A і B та містить елементи, які належать тільки одній із цих множин

4. `(A \ B) ∸ (B \ A) = ∅`
   
 Множини A \ B і B \ A не мають спільних елементів, тому:
(A \ B) ∩ (B \ A) = ∅

Для неперетинних множин симетрична різниця збігається з їх об'єднанням:
(A \ B) ∸ (B \ A) = (A \ B) ∪ (B \ A)

А за означенням:
(A \ B) ∪ (B \ A) = A ∸ B

Тому з умови маємо:
A ∸ B = ∅

Симетрична різниця дорівнює порожній множині тільки тоді, коли множини рівні:
A = B

Відповідь: множини A і B рівні

5. `(A ∪ B) ∸ A = B`
   
 За означенням симетричної різниці:
((A ∪ B) \ A) ∪ (A \ (A ∪ B)) = B

Оскільки A ⊆ A ∪ B, маємо:

A \ (A ∪ B) = ∅

А:
(A ∪ B) \ A = B \ A

Тому:

(B \ A) ∪ ∅ = B
B \ A = B

Це означає, що жоден елемент B не належить одночасно A

Отже:
A ∩ B = ∅

Відповіьь: множини A і B не мають спільних елементів

### 3. Довести тотожність

Довести, що для будь-яких множин `A` і `B`:

`A ∪ B = A ∪ (B \ A) = (A \ B) ∪ (A ∩ B) ∪ (B \ A)`,

і що три множини у правій частині останнього виразу попарно не перетинаються,
тобто це розбиття `A ∪ B` на три неперетинні частини

Спочатку розглянемо другу частину:
A ∪ (B \ A)
= A ∪ (B ∩ Ā)
(B \ A = B ∩ Ā)
= A ∪ B
(закон поглинання)

Тому:
A ∪ B = A ∪ (B \ A)

Тепер розглянемо:
(A \ B) ∪ (A ∩ B) ∪ (B \ A)

Замінюємо різницю множин:
= (A ∩ B̄) ∪ (A ∩ B) ∪ (B ∩ Ā)
(A \ B = A ∩ B̄, B \ A = B ∩ Ā)

Перші дві частини об'єднуємо за законом склеювання:
= A ∪ (B ∩ Ā)
= A ∪ B
(закон поглинання)

Отже:
A ∪ B = A ∪ (B \ A) = (A \ B) ∪ (A ∩ B) ∪ (B \ A)

Тепер покажемо, що три множини в останній частині попарно не перетинаються

Перша пара:
(A \ B) ∩ (A ∩ B) = ∅

Елемент не може одночасно належати B і не належати B

Друга пара:
(A \ B) ∩ (B \ A) = ∅

Елемент не може одночасно належати A і не належати A

Третя пара:
(A ∩ B) ∩ (B \ A) = ∅

Елемент з A ∩ B обов'язково належить A, а елемент з B \ A не належить A

Отже:
(A \ B) ∩ (A ∩ B) = ∅
(A \ B) ∩ (B \ A) = ∅
(A ∩ B) ∩ (B \ A) = ∅

Таким чином, множини A \ B, A ∩ B та B \ A попарно не перетинаються, а їх об'єднання дорівнює A ∪ B

Відповідь: A ∪ B розбивається на три неперетинні частини:
A \ B, A ∩ B, B \ A
