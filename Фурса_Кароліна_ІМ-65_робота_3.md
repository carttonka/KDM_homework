# Домашнє завдання 3. Множини: рівняння

**Тема:** Теорія множин

## Завдання

Розв'язати рівняння (там, де вказана додаткова умова — врахувати її):

1. `A ∸ B = B ∸ X`
2. Відповідь: A ⊆ B, B ⊆ X.

Тому що ліва частина A ∸ B повинна бути рівна правій частині B ∸ X. Якщо A має елементи, яких немає в B, вони будуть тільки зліва.
Тому A ⊆ B.
Тоді ліва частина стає порожньою множиною. Щоб права частина також була порожньою, потрібно B ⊆ X

3. `A ∪ X = X \ B`
4.  Відповідь: якщо A ∩ B = ∅, то A ⊆ X ⊆ B̄. Якщо A ∩ B ≠ ∅, розв'язків немає

Тому що X \ B є підмножиною X, тому зліва A теж повинна бути підмножиною X.
Отже, A ⊆ X.
Тоді ліва частина дорівнює X, і маємо X = X \ B.
Це можливо тільки тоді, коли X не має спільних елементів з B, тобто X ⊆ B̄.
Тому A також не повинна мати спільних елементів з B

5. `C \ B = (A ∪ B) \ X`, `A ⊆ C`
6. Відповідь: якщо C \ B ⊆ A, то
(A ∪ B) \ (C \ B) ⊆ X ⊆ \overline{(C \ B)}

Якщо C \ B ⊄ A, розв'язків немає.
Тому що ліва частина містить елементи C, які не належать B.
Щоб вони були і в правій частині, вони повинні належати A, тому потрібна умова C \ B ⊆ A.
Елементи з C \ B не повинні належати X, інакше вони зникнуть з правої частини.
Водночас усі інші елементи з A ∪ B повинні належати X.
Звідси отримуємо вказані межі для X

8. `X \ A = C \ X`
9. Відповідь: якщо C ⊆ A, то C ⊆ X ⊆ A. Якщо C ⊄ A, розв'язків немає

Тому що елементи C, які не належать A, неможливо розподілити між двома частинами так, щоб вони були рівними
Тому потрібно C ⊆ A.
Після цього, щоб права частина була порожньою, всі елементи C повинні належати X.
А щоб ліва частина була порожньою, X не повинна містити елементів поза A
Отже, C ⊆ X ⊆ A

10. `A ∸ C = C \ A`, `A ⊆ C`
11. Відповідь: X - будь-яка множина

Тому що X у цьому рівнянні взагалі не використовується. За умовою A ⊆ C, тому A ∸ C = C \ A.
Оттже, рівність виконується незалежно від того, якою є X

12. `A \ (X \ C) = A ∪ C`
13. Відповідь: C ⊆ A і X ⊆ Ā ∪ C

Тому що ліва частина є підмножиною A, а справа є A ∪ C.
Тому C повинна бути підмножиною А.
Після цього маємо A ∪ C = A. Щоб з A нічого не забиралося, елементи X, які належать A, повинні також належати C.
Тому X ⊆ Ā ∪ C

14. `(A \ X) \ C = A ∪ C`
15. Відповідь: A = ∅, C = ∅, X - будь-яка множина

Тому що ліва частина не може містити елементи, які належать C, а права частина містить усі елементи A і C.
Тому для рівності обидві множини A і C повинні бути порожніми.
Після цього обидві частини дорівнюють ∅, тому X може бути будь-якою множиною

16. `A ∸ X = C \ X`
17. Відповідь: якщо C ⊆ A, то A \ C ⊆ X ⊆ A. Якщо C ⊄ A, розвязків немає

Тому що в лівій частині A ∸ X не повинно залишатися елементів X, які не належать A.
Тому X ⊆ A. Далі всі елементи A, які не належать C, повинні потрапити в X, тому A \ C ⊆ X.
Також потрібно C ⊆ A, інакше елементи C, які не належать A, будуть тільки справа

18. `(A ∩ X) ∩ (C \ X) = A ∩ C`
19. Відповідь: якщо A ∩ C = ∅, то X - будь-яка множина. Якщо A ∩ C ≠ ∅, розв'язків немає

Тому що C \ X = C ∩ X̄. Тоді ліва частина містить X ∩ X̄, а це завжди ∅.
Отже, ліва частина завжди дорівнює ∅. Щоб рівність виконувалась, права частина A ∩ C також повинна бути порожньою.
Тому потрібно A ∩ C = ∅.
Якщо це виконується, X може бути будь-якою множиною
