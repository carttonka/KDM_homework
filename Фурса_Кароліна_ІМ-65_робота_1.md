# Домашнє завдання 1. Множини: базові поняття, рівність, включення

**Тема:** Теорія множин

## Завдання

### 1. Які з наведених нижче співвідношень неправильні й чому?

1. `x ∈ {2, a, x}`
2. Відповідь: Правильне, тому що x є одним з елементів множини {2, a, x}.
3. `3 ∈ {1, {2, 3}, 4}`
4. Відповідь: Неправильне, бо 3 не є окремим елементом множини. Воно знаходиться всередині {2, 3}. Елементами цієї множини є 1, {2, 3} і 4.
5. `x ∈ {1, sin x}`
6. Відповідь: Правильне тільки для деяких значень x. Наприклад, при x = 0 воно правильне, бо sin 0 = 0. Для інших значень це може бути неправильно.
7. `{x, y} ∈ {a, {x, y}, b}`
8. Відповідь: Правильне, бо {x, y} є одним з елементів множини {a, {x, y}, b}.

### 2. Чи рівні між собою множини A і B? Якщо ні — чому?

1. `A = {2, 5, 4}`, `B = {5, 4, 2}`
2. Відповідь: A = B, бо порядок елементів у множині не має значення. В обох множинах є 2, 4 і 5.
3. `A = {1, 2, 4, 2}`, `B = {1, 2, 4}`
4. Відповідь: A = B, бо однакові елементи в множині не повторюються. Тому два числа 2 у A рахуються як одне.
5. `A = {2, 4, 5}`, `B = {2, 4, 3}`
6. Відповідь: A ≠ B, бо в A є 5, а в B замість нього є 3. Отже, елементи множин різні.
7. `A = {1, {2, 5}, 6}`, `B = {1, {5, 2}, 6}`
8. Відповідь: A = B, бо {2, 5} і {5, 2} містять однакові елементи. Порядок їх запису не має значення.
9. `A = {1, {2, 5}, 6}`, `B = {1, 2, 5, 6}`
10. Відповідь: A ≠ B, бо в A {2, 5} є одним елементом, а в B 2 і 5 є окремими елементами.

### 3. Чи пов'язані множини A і B відношенням включення? Якщо так — яка з них підмножина іншої?

1. `A = {a, b, d}`, `B = {a, b, c, d}`
2. Відповідь: A ⊆ B, бо всі елементи A є також у B. Тому A є підмножиною B.
3. `A = {a, c, d, e}`, `B = {a, e, c}`
4. Відповідь: B ⊆ A, бо всі елементи B є в A. Але A не є підмножиною B, бо в A є ще d.
5. `A = {c, d, e}`, `B = {c, a}`
6. Відповідь: A не є підмножиною B, бо d і e немає в B. B теж не є підмножиною A, бо a немає в A.
7. `A = {1, {2, 3}}`, `B = {1, 2, 3}`
8. Відповідь: A і B не є підмножинами одна одної. У A {2, 3} є одним елементом, а в B 2 і 3 є окремими елементами.
9. `A = {1, {2, {3}}}`, `B = {1, 4, {2, {3}}}`
10. Відповідь: A ⊆ B, бо всі елементи A є в B. Але A ≠ B, бо в B є ще один елемент 4.

### 4. У яких відношеннях перебувають між собою три множини?

`A = {1, 3}`; `B` — множина непарних додатних чисел; `C` — множина розв'язків рівняння `x² − 4x + 3 = 0`.

Відповідь: Розв'яжемо рівняння x² − 4x + 3 = 0.
(x − 1)(x − 3) = 0
x = 1 або x = 3, отже C = {1, 3}.

Оскільки A = {1, 3}, то A = C. Числа 1 і 3 є додатними непарними числами, тому вони є в B.
Отже, A = C ⊂ B.

### 5. Свята та вихідні

Утворіть множину святкових (неробочих) днів поточного року. Чи перетинається ця множина з множиною вихідних днів (субот і неділь) цього ж року? Якщо так — випишіть елементи перетину (дати, коли святковий день випадає на суботу чи неділю).

Відповідь: У 2026 році на суботу або неділю припадають такі святкові дні:

08.03.2026 Міжнародний жіночий день
12.04.2026 Великдень
31.05.2026 Трійця
28.06.2026 День Конституції України

Отже:
A ∩ B = {08.03.2026, 12.04.2026, 31.05.2026, 28.06.2026}
