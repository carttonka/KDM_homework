# Домашнє завдання 4. Множини: системи рівнянь

**Тема:** Теорія множин

## Завдання

Розв'яжіть наступні системи рівнянь відносно `X` (кожна система складена з
двох рівнянь ДЗ 3 — за потреби перевикористайте проміжні результати звідти):

1. `{ A ∸ B = B ∸ X;  X \ A = C \ X }`
2. Відповідь: якщо A = B і C ⊆ A, то X = A

Тому що з першого рівняння маємо A ⊆ B і B ⊆ X. 
З другого рівняння маємо C ⊆ X ⊆ A. 
Отже, разом отримуємо A ⊆ B ⊆ X ⊆ A. Тосу A = B = X.
Також із другого рівняння залишається умова C ⊆ A

3. `{ A ∪ X = X \ B;  A \ (X \ C) = A ∪ C }`
4. Відповідь: якщо A = C і A ∩ B = ∅, то A ⊆ X ⊆ B̄

Тому що перше рівняння дає умови A ∩ B = ∅ і A ⊆ X ⊆ B̄.
Друге рівняння дає C ⊆ A і X ⊆ Ā ∪ C.
Оскільки A ⊆ X, для виконання обох умов потрібно, щоб A ⊆ C. 
Разом з C ⊆ A отримуємо A = C.
Тому остаточно A ⊆ X ⊆ B̄

5. `{ C \ B = (A ∪ B) \ X, A ⊆ C;  (A \ X) \ C = A ∪ C }`
6. Відповідь: якщо A = ∅ і C = ∅, то B ⊆ X

Тому що друге рівняння має вигляд (A \ X) \ C = A ∪ C.
Ліва частина є підмножиною A, а права частина містить A і C, тому рівність можлива тільки тоді, коли A = ∅ і C = ∅.
Тоді перше рівняння стає ∅ = B \ X, звідки B ⊆ X

7. `{ A ∸ C = C \ A, A ⊆ C;  A ∸ X = C \ X }`
8. Відповідь: якщо A = C, то X ⊆ A

Тому що перше рівняння разом з умовою A ⊆ C виконується.
Друге рівняння A ∸ X = C \ X потребує умови C ⊆ A.
Разом з A ⊆ C отримуємо A = C. 
Після цього маємо A ∸ X = A \ X, що виконується тоді, коли X ⊆ A

9. `{ X \ A = C \ X;  (A ∩ X) ∩ (C \ X) = A ∩ C }`
10. Відповідь: якщо C = ∅, то X ⊆ A

Тому що перше рівняння дає C ⊆ X ⊆ A. 
Друге рівняння має зліва X ∩ X̄, тому його ліва частина завжди дорівнює ∅.
Отже, потрібно A ∩ C = ∅. Разом з умовою C ⊆ A це можливо тільки тоді, коли C = ∅. 
Тоді з першого рівняння залишається X ⊆ A
